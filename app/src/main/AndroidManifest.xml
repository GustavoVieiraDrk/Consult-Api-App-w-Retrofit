<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gustavo.project_rxjava_retrofit_mark00">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectRxjavaretrofitmark00">
        <activity
            android:name=".Anime.Activities.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".Anime.Activities.MainActivity"
            android:exported="true" />
        <activity android:name=".Anime.Activities.ShowCharacterDetailsActivity" />
        <activity android:name=".Anime.Activities.LoginScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>